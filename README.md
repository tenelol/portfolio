# nixar template

Minimal starter template using nixar with a tiny site and JSON endpoint.

## Run
```bash
nix run
```

## Develop
```bash
nix develop
```

## Build
```bash
nix build
```

## Routes
- `/` Home page
- `/about` About page
- `/api/hello` JSON sample
- `/health` Health check
- `/static/*` Static files
