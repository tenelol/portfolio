root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  include_ext = ["go", "html", "css"]
  exclude_dir = ["node_modules", "tmp"]
  stop_on_error = true

[log]
  time = true
